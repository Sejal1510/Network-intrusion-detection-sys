# Installed fresh inside the build stage (npm ci) -- copying the host's
# node_modules in would drag in a wrong-platform build of any native
# dependency (e.g. rollup's optional binaries).
node_modules/
dist/
.vite/
coverage/
.env
.env.local
*.local
