"""Inference API: serves one trained run (see nids.training.artifacts) as a
prediction service.

This package only *consumes* nids.data/features/models/training -- it adds
no new persistence format, feature logic, or model logic of its own.
"""
